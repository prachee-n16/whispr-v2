{
    "version": 2,
    "builds": [
        {
            "src": "./server.js",
            "use": "@vercel/node"
        }
    ],
    "buildCommand": "nodemon --experimental-specifier-resolution=node server",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}